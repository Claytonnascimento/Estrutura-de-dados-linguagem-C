#include <stdio.h>

int main() {
    int num, i, soma = 0;
    scanf("%d", &num);

    for(i=1; i<=num; i++) {
        if(i % 2 == 0)
            soma += i;
    }

    printf("Soma dos pares: %d\n", soma);
    return 0;
}
