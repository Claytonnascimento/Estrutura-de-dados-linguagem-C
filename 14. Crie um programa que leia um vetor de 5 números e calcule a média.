#include <stdio.h>

int main() {
    int v[5], i;
    int soma = 0;

    for(i=0; i<5; i++) {
        scanf("%d", &v[i]);
        soma += v[i];
    }

    printf("Media: %.2f\n", soma/5.0);
    return 0;
}
