#include <stdio.h>

int ehPar(int num) {
    return num % 2 == 0;
}

int main() {
    int num;
    scanf("%d", &num);

    if(ehPar(num))
        printf("Par\n");
    else
        printf("Impar\n");

    return 0;
}
