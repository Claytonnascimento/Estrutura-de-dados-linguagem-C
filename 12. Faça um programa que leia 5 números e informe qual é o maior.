#include <stdio.h>

int main() {
    int num, maior, i;

    scanf("%d", &maior);

    for(i=1; i<5; i++) {
        scanf("%d", &num);
        if(num > maior)
            maior = num;
    }

    printf("Maior: %d\n", maior);
    return 0;
}
